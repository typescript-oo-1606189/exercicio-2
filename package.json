{
  "name": "exercicio-2",
  "version": "1.0.0",
  "description": "## Objetivo Demonstrar o uso de `enums` para categorizar valores, `functions` para manipulação de objetos e o uso de `type` para definir a estrutura dos objetos em TypeScript.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
